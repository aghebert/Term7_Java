
public class BulkDiscount extends DiscountPolicy {

	BulkDiscount(){
		float minimum;
		double percent;
	}
	

	@Override
	double computeDiscount(int count, float itemCost) {
		double discount = 0;
		if(count >= minimum) {
			count * itemCost
			return discount;
		}
		return 0;
	}

}
